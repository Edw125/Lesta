# Наивный алгоритм(Медленнее, чем побитовые операции)
# def isEven(value):
#     return value % 2 == 0


# Хитрый алгоритм (Побитовое И используется для выключения битов. В результате четные числа преобразуются в 0,
# нечетные в 1. Это работа напрямую с внешними микросхемами - максимально быстрые вычисления)
def isEvenMod(value):
    return value & 1 == 0


print(isEvenMod(13))
